# KISEC Cyber Escape 디자인 시스템

실제 게임 화면과 스타일 가이드가 같은 기준을 사용하도록 `src/index.css`의 `--ui-*` 토큰을 단일 기준으로 삼습니다.

개발 서버 주소 뒤에 `?design-system`을 붙이면 실제 토큰으로 렌더링한 확인 화면을 볼 수 있습니다.

예: `http://localhost:8443/?design-system`

## 방향

- 어두운 서버실 위에 배치되는 명확한 게임 HUD
- 흰색·블루그레이 임무 패널과 딥 네이비 HUD의 두 가지 표면
- 파란색은 진행과 행동, 골드는 획득 보상, 빨간색은 오류에만 사용
- 둥근 모바일 앱 카드가 아니라 두께가 느껴지는 게임 패널과 버튼
- 장식보다 정보 위계를 우선하고, 발광은 선택·잠금·획득 상태에만 제한

## 핵심 토큰

| 역할 | 토큰 | 값 |
| --- | --- | --- |
| 한글 UI | `--ui-font` | Noto Sans KR |
| 숫자·짧은 영문 | `--ui-font-number` | Rubik + Noto Sans KR |
| 밝은 패널 | `--ui-surface` | `#F8FBFF` |
| 보조 표면 | `--ui-surface-muted` | Blue 5 |
| 어두운 HUD | `--ui-surface-dark` | Blue 95 |
| 기본 본문 | `--ui-text` | Blue 95 |
| 보조 본문 | `--ui-text-muted` | `#536B88` |
| 기본 테두리 | `--ui-border` | Blue 20 |
| 활성 테두리 | `--ui-border-active` | Blue 30 |
| 작은 모서리 | `--ui-radius-sm` | 8px |
| 컨트롤 모서리 | `--ui-radius-md` | 12px |
| 패널 모서리 | `--ui-radius-lg` | 18px |
| 강조 패널 모서리 | `--ui-radius-xl` | 22px |

## 타이포그래피

- 모든 한글 제목, 본문, 버튼은 `--ui-font`를 사용합니다.
- 숫자, 점수, 레벨과 짧은 영문만 `--ui-font-number`를 사용합니다.
- 한 화면에서 굵기는 본문 500~600, 라벨 700, 핵심 제목 800의 세 단계만 사용합니다.
- 한글 자간은 기본값 또는 `-0.04em` 이내로 제한합니다.

## 컴포넌트 규칙

### 패널

- 밝은 임무 패널은 `--ui-surface`, Blue 20~30 테두리, 18~22px 모서리를 사용합니다.
- HUD는 Blue 95 계열 불투명 배경과 8~12px 모서리를 사용합니다.
- 패널 안에 같은 모양의 카드를 반복하지 않고 구분선과 간격으로 위계를 만듭니다.

### 버튼

- 핵심 행동은 `--ui-gradient-action`과 `--ui-shadow-control`을 사용합니다.
- 눌림 상태는 4~5px 아래로 이동하고 그림자를 `--ui-shadow-control-pressed`로 줄입니다.
- 한 화면의 핵심 행동 버튼은 한 개만 둡니다.

### 상태

- 정답과 완료는 기존 파란색 시스템 안에서 표현합니다.
- 오답과 오류만 `--ui-gradient-danger` 및 빨간색 계열을 사용합니다.
- 잠금과 완료 아이콘은 같은 48px 프레임, 12px 외곽 모서리, 8px 내부 모서리를 공유합니다.

### 보상

- 골드와 별 이미지는 점수·획득·완료 화면에만 사용합니다.
- 별은 사용자가 제공한 `public/result-rank-star.svg`를 원본으로 사용합니다.
- 과도한 반짝임을 추가하지 않고 원본 입체감과 짧은 획득 애니메이션만 유지합니다.
- 정답률 0~39%는 별 0개, 40~69%는 1개, 70~89%는 2개, 90~100%는 3개를 지급합니다.

## 화면별 적용 기준

- 타이틀·닉네임: 같은 파란 입체 버튼과 22px 강조 패널
- 온보딩: 밝은 대화 패널과 공통 액션 블루 이름표
- 탐색 HUD: 딥 네이비 표면, 12px 컨트롤, 제한된 발광
- 퀘스트 팝업: 밝은 임무 패널, 제목 우선, 단일 실행 버튼
- 문제 풀이·키패드: 12px 컨트롤과 동일한 정답·오답 상태
- 결과: 큰 별, 정답·경험치·정답률, 단일 완료 버튼만 표시

## 피해야 할 표현

- 화면마다 다른 폰트나 임의의 보라색·초록색 추가
- 모든 정보를 각각 둥근 카드 안에 넣는 구성
- 파스텔 그라데이션, 글래스모피즘, 과도한 네온과 발광
- 의미 없이 크기만 큰 배지, 반복되는 영문 라벨, 장식용 아이콘
- 같은 역할인데 모서리·그림자·눌림 방식이 다른 버튼
